PORT=3002
DATABASE_URL=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Comma-separated list of frontend origins permitted by CORS.
# Default (when unset): http://localhost:3001  — local dev frontend port.
# Production: set to the real frontend origin, e.g. https://app.example.com
# Multiple origins: https://app.example.com,https://staging.example.com
CORS_ALLOWED_ORIGINS=http://localhost:3001
