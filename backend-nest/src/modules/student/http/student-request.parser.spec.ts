import { StudentRequestParser } from './student-request.parser';
describe('StudentRequestParser',()=>{const parser=new StudentRequestParser();it('keeps legacy page parsing, omitted invalid pins, and exclude aliases',()=>{expect(parser.parsePage({page:'1x',size:'2',toplist:'3,bad',exclude_ids:['4','x']})).toEqual(expect.objectContaining({page:1,size:2,toplist:[3],excludeIds:[4]}));expect(()=>parser.parsePage({page:'0',size:'2'})).toThrow(expect.objectContaining({code:'C601'}));expect(()=>parser.parseId('bad')).toThrow(expect.objectContaining({code:'D601'}));});it('normalizes multipart values and validates create/password and strict trash ids',()=>{expect(parser.parseWrite({code:'SV',fullname:'Student',email:'s@example.test',username:'s',password:'Valid1!x',sex:'false',class_id:'-1',hobbies:''},true,0)).toMatchObject({sex:false,class_id:null,hobbies:0});expect(()=>parser.parseWrite({code:'SV',fullname:'Student',email:'s@example.test',username:'s',password:'bad'},true,0)).toThrow(expect.objectContaining({code:'E603'}));expect(parser.parseTrashIds([4,'4',8])).toEqual([4,8]);});});
